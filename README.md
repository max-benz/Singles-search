# single-search

This repository holds:
- `extraction/` → our GW Apps → Supabase sync script  
- `frontend/`   → our Next.js search UI  
- `.github/workflows/sync.yml` → the GitHub Actions job that runs the sync daily
